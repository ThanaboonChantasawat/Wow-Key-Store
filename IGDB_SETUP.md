# IGDB API Setup Guide

เอกสารนี้แนะนำวิธีการตั้งค่า IGDB API เพื่อดึงข้อมูลเกมยอดนิยมในระบบ Admin

## 📋 ขั้นตอนการตั้งค่า

### 1. สร้าง Twitch Developer Application

IGDB ใช้ระบบ OAuth ของ Twitch ดังนั้นคุณต้องสร้างแอปใน Twitch Developer Console ก่อน

1. ไปที่ **[Twitch Developer Console](https://dev.twitch.tv/console/apps)**
2. ล็อกอินด้วยบัญชี Twitch (ถ้ายังไม่มีให้สมัครใหม่ที่ [twitch.tv](https://twitch.tv))
3. คลิกปุ่ม **"Register Your Application"**
4. กรอกข้อมูลดังนี้:
   - **Name**: ชื่อแอปของคุณ (เช่น `WowKeyStore IGDB Integration`)
   - **OAuth Redirect URLs**: `http://localhost` (สำหรับ development)
   - **Category**: เลือก `Website Integration` หรือ `Application Integration`
5. คลิก **"Create"**

### 2. รับ Client ID และ Client Secret

1. หลังจากสร้างแอปแล้ว คลิกที่ชื่อแอปเพื่อดูรายละเอียด
2. คุณจะเห็น **Client ID** (แสดงอยู่แล้ว)
3. คลิกปุ่ม **"New Secret"** เพื่อสร้าง **Client Secret**
4. **⚠️ สำคัญ**: คัดลอก Client Secret ทันที เพราะจะแสดงเพียงครั้งเดียว

### 3. เพิ่ม Environment Variables

เปิดไฟล์ `.env.local` ที่ root ของโปรเจกต์ แล้วแก้ไขค่าเหล่านี้:

```bash
# IGDB API (Twitch Developer Console)
IGDB_CLIENT_ID=your_actual_client_id_here
IGDB_CLIENT_SECRET=your_actual_client_secret_here
```

แทนที่:
- `your_actual_client_id_here` ด้วย **Client ID** ที่คุณได้จาก Twitch
- `your_actual_client_secret_here` ด้วย **Client Secret** ที่คุณได้จาก Twitch

### 4. รีสตาร์ท Development Server

หลังจากแก้ไข `.env.local` แล้ว ต้องรีสตาร์ทเซิร์ฟเวอร์:

```powershell
# หยุด server ปัจจุบัน (กด Ctrl+C)
# แล้วรันใหม่
npm run dev
```

### 5. ทดสอบการทำงาน

1. เข้าสู่ระบบในฐานะ Admin
2. ไปที่ **Admin Dashboard** → **จัดการเกมยอดนิยม**
3. ดูการ์ด **"เกมยอดนิยมจาก IGDB"** ทางด้านขวา
4. ควรเห็นรายการเกมยอดนิยมจาก IGDB แสดงขึ้นมา
5. หากยังไม่แสดง ลองกดปุ่ม **"รีเฟรช"**

## 🔍 Troubleshooting

### ❌ Error: "Missing IGDB_CLIENT_ID or IGDB_CLIENT_SECRET"

**สาเหตุ**: ไม่ได้ตั้งค่า environment variables หรือยังไม่ได้รีสตาร์ทเซิร์ฟเวอร์

**วิธีแก้**:
1. ตรวจสอบว่าไฟล์ `.env.local` มีค่า `IGDB_CLIENT_ID` และ `IGDB_CLIENT_SECRET`
2. ตรวจสอบว่าไม่มี typo ในชื่อ environment variable
3. รีสตาร์ท development server (`Ctrl+C` แล้ว `npm run dev` ใหม่)

### ❌ Error: "Failed to get Twitch access token: 401"

**สาเหตุ**: Client ID หรือ Client Secret ไม่ถูกต้อง

**วิธีแก้**:
1. กลับไปตรวจสอบค่าใน Twitch Developer Console
2. ลองสร้าง Client Secret ใหม่
3. อัปเดตค่าใน `.env.local` ให้ตรงกับค่าใหม่
4. รีสตาร์ทเซิร์ฟเวอร์

### ❌ Error: "Failed to fetch IGDB games: 403"

**สาเหตุ**: แอป Twitch ยังไม่ได้รับสิทธิ์เข้าถึง IGDB API

**วิธีแก้**:
1. ตรวจสอบว่าแอปใน Twitch Developer Console ยังคง active อยู่
2. ลองสร้างแอปใหม่ถ้าจำเป็น
3. รอสักครู่ (บางครั้ง Twitch ต้องใช้เวลาในการ sync)

### ℹ️ ดูข้อความ Error แบบละเอียด

1. เปิด **Browser DevTools** (กด F12)
2. ไปที่แท็บ **Console**
3. ดู log ที่ขึ้นต้นด้วย `❌ Error fetching IGDB popular games:`
4. ดู log `IGDB popular API error response:` เพื่อดูรายละเอียด error จาก API

## 📚 ข้อมูลเพิ่มเติม

- [IGDB API Documentation](https://api-docs.igdb.com/)
- [Twitch Authentication Guide](https://dev.twitch.tv/docs/authentication)
- [IGDB Account Setup](https://api.igdb.com/signup)

## 🎮 คุณสมบัติที่ได้หลังตั้งค่าเสร็จ

- ✅ ดูเกมยอดนิยมจากฐานข้อมูล IGDB แบบเรียลไทม์
- ✅ ดูข้อมูล: ชื่อเกม, ปีวางจำหน่าย, จำนวนรีวิว, ประเภทเกม
- ✅ รีเฟรชข้อมูลได้ด้วยการกดปุ่ม
- ✅ ใช้เป็นข้อมูลอ้างอิงในการเลือกเกมยอดนิยมสำหรับระบบ

---

**หมายเหตุ**: ข้อมูลจาก IGDB เป็นแบบ read-only และใช้เป็นข้อมูลอ้างอิงเท่านั้น ไม่ได้เชื่อมโยงโดยตรงกับเกมในระบบ `gamesList` ของคุณ
